<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="gc-tjara.list">
      <field name="name">GC-TJARA list</field>
      <field name="model">gc-tjara.gc-tjara</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="gc-tjara.action_window">
      <field name="name">GC-TJARA window</field>
      <field name="res_model">gc-tjara.gc-tjara</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="gc-tjara.action_server">
      <field name="name">GC-TJARA server</field>
      <field name="model_id" ref="model_gc-tjara_gc-tjara"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="GC-TJARA" id="gc-tjara.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="gc-tjara.menu_1" parent="gc-tjara.menu_root"/>
    <menuitem name="Menu 2" id="gc-tjara.menu_2" parent="gc-tjara.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="gc-tjara.menu_1_list" parent="gc-tjara.menu_1"
              action="gc-tjara.action_window"/>
    <menuitem name="Server to list" id="gc-tjara" parent="gc-tjara.menu_2"
              action="gc-tjara.action_server"/>
    -->
  </data>
</odoo>